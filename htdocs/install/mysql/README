# README
########

This directory contains:

data:      A directory with SQL script to add Gestimag initial data.
migration: A directory with SQL script to migrate Gestimag database from 
           one version to another version.
tables:    A directory with all files with create tables and index orders.
